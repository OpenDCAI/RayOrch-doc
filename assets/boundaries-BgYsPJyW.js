import{A as e,d as t,p as n}from"./vendor~app~SearchBox~404.html~multi-node~installation~index~first-pipeline~fan-out-and-redu~imzjopqm-3b7AYI2B.js";import{t as r}from"./app-BVohhq9J.js";var i=JSON.parse(`{"path":"/en/guide/boundaries.html","title":"Capabilities and Boundaries","lang":"en-US","frontmatter":{"title":"Capabilities and Boundaries"},"readingTime":{"minutes":1.44,"words":432},"git":{"createdTime":1789754035000,"updatedTime":1789754035000,"contributors":[{"name":"Sunnyhaze","username":"Sunnyhaze","email":"mxch1122@126.com","commits":1,"avatar":"https://avatars.githubusercontent.com/Sunnyhaze?v=4","url":"https://github.com/Sunnyhaze"}]},"filePathRelative":"en/guide/boundaries.md","headers":[]}`),a={name:`boundaries.md`};function o(r,i,a,o,s,c){return e(),t(`div`,null,[...i[0]||=[n(`<p>RayOrch is deliberately a small orchestration layer. It coordinates finite, multi-stage AI workloads on Ray; it does not try to replace the execution engines and infrastructure around them.</p><h2 id="what-rayorch-provides" tabindex="-1"><a class="header-anchor" href="#what-rayorch-provides"><span>What RayOrch provides</span></a></h2><ul><li>A static, acyclic <code>Pipeline</code> that makes stage dependencies explicit.</li><li>Ordinary batched Python UDFs hosted by persistent Ray actors.</li><li>Completion-driven dispatch: downstream work starts as soon as its own inputs are ready.</li><li>Explicit <code>expand</code>, <code>filter</code>, <code>broadcast</code>, and <code>reduce</code> operations for cardinality and lineage.</li><li>Per-stage CPU, GPU, replica, batching, and Ray <code>runtime_env</code> configuration.</li><li>Multi-node and multi-GPU execution through an existing Ray cluster.</li><li>Bounded recovery policies, failure-aware result reconstruction, and internal deadlock detection.</li><li>Lazy Benchmark registration, configurable runs, Ray Job submission, and reproducible reports.</li></ul><h2 id="what-remains-the-user-s-or-platform-s-responsibility" tabindex="-1"><a class="header-anchor" href="#what-remains-the-user-s-or-platform-s-responsibility"><span>What remains the user&#39;s or platform&#39;s responsibility</span></a></h2><ul><li><strong>Models and computation:</strong> vLLM, SGLang, PyTorch, and other engines still perform inference or processing.</li><li><strong>Cluster operations:</strong> Ray still owns node discovery, placement, transport, and resource accounting.</li><li><strong>Environment provisioning:</strong> RayOrch selects a stage&#39;s <code>runtime_env</code>; it does not create matching Conda environments on every node.</li><li><strong>Data distribution:</strong> model weights, datasets, and output paths must already be reachable from the nodes that use them.</li><li><strong>External-call timeouts:</strong> RayOrch detects an impossible internal transition, but cannot interrupt arbitrary UDF code that blocks forever. Configure timeouts in model clients and external services.</li><li><strong>Idempotent side effects:</strong> a recovery policy may invoke a UDF again. Writes to external systems must therefore be idempotent when retries are enabled.</li></ul><h2 id="workloads-outside-the-current-model" tabindex="-1"><a class="header-anchor" href="#workloads-outside-the-current-model"><span>Workloads outside the current model</span></a></h2><p>The current runtime is designed for finite, row-aligned inputs and a static acyclic graph. It does not currently provide:</p><ul><li>unbounded streaming, windows, or long-running stream checkpoints;</li><li>dynamic graph mutation or feedback loops;</li><li>arbitrary cross-input joins;</li><li>exactly-once guarantees for external side effects;</li><li>automatic construction of backend-specific environments;</li><li>a guarantee that every workload is faster than direct Ray code.</li></ul><p>RayOrch is most useful when a workload has several heterogeneous stages, irregular fan-out or fan-in, expensive persistent models, and enough independent work for completion-driven scheduling and batching to overlap effectively.</p><h2 id="operational-timeout-model" tabindex="-1"><a class="header-anchor" href="#operational-timeout-model"><span>Operational timeout model</span></a></h2><p>Three different cases should not be confused:</p><ol><li><strong>Internal semantic deadlock:</strong> RayOrch raises an error when active input batches cannot make progress and no RPC is pending.</li><li><strong>A blocked UDF or external service:</strong> configure a timeout inside that UDF or client.</li><li><strong>Waiting for a submitted Ray Job:</strong> <code>BenchmarkRun.wait(timeout_s=...)</code> limits the client wait only. Call <code>run.stop()</code> if the remote Job should also be terminated.</li></ol><p>This separation keeps the runtime contract explicit instead of pretending one global timeout can safely govern every model and external system.</p>`,13)]])}var s=r(a,[[`render`,o]]);export{i as _pageData,s as default};